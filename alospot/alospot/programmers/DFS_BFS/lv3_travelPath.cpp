#include <iostream>
#include <string>
#include <vector>
#include <algorithm>

using namespace std;
bool check[10000];
vector<string> ans;

vector<string> dfs(string from, string to, vector<vector<string>> tickets, int n)
{
	if (n == tickets.size())
	{
		ans.push_back(to);
		return ans;
	}
}

vector<string> solution(vector<vector<string>> tickets) {
	vector<string> answer;
	return answer;
}

int main(void)
{
	

	return 0;
}